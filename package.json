{
  "name": "hrmapp",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=icon.icns --prune=true --out=release-builds",
    "package-win": "electron-packager . MyApp --platform=win32 --arch=x64 --out=release-builds --icon=icon.ico --win32metadata.CompanyName='OnestTech' --win32metadata.ProductName='Onest HRM'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron-packager": "^17.1.1"
  },
  "devDependencies": {
    "electron": "^25.0.0",
    "electron-icon-maker": "^0.0.5",
    "icon-gen": "^3.0.1",
    "mkdirp": "^3.0.1"
  }
}
